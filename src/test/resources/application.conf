db {
    jdbcUrl = "jdbc:h2:mem:test;DATABASE_TO_UPPER=false;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS user"
    dbUser = root
    dbPassword = root
    schema = user_mgmt
    driver = "org.h2.Driver"
}

auth {
 secretKey=898748674728934843
 jwtSecret=898748674728934843
}
